apiVersion: v2
name: sample-app
description: Production Helm chart for the sample-app (Spring Boot) service
type: application
version: 1.0.0        # chart version — bump on template changes
appVersion: "2.1.2"   # app version — overridden per-deploy by image.tag
